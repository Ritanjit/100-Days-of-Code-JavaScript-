
/* Storing variable in a Function */

function calcAverage(a,b,c){
    return (a+b+c)/3;
  }
  
  let avg = calcAverage(83,65,90);
  
  console.log("\n\n Your Average Score : " + avg.toFixed(2));
  
  // TIP : toFixed(n) decides the number of decimal places 
  
  