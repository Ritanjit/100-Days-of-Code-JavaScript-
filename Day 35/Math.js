
/* Math.random() functin generates a number between 0 (inclusive) and 1 (exclusive), meaning it can have 0 but not 1 */

// Random Number Generator
function randomFraction(){
    return Math.random();
}

let random = randomFraction();
console.log(random);