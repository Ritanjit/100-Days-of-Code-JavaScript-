/* The 'const' keyword --> 'let' and more..... */

// the const keywprd cannot be reassigned to a new value

const APPLE = "\n Apples are RED! \n"; //initial value

console.log(APPLE);

APPLE="\n APPLES are GREEN !"; //new value

console.log(APPLE);

// Tip: We use Uppercase for values that cannot be reassigned.