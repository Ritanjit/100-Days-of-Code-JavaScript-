/* boo-whoo boolean primmitive in JavaScript */

// check if input has boolean value of true or false

function booWho(bool) {
    if (bool === true || bool === false) {
        return true;
    }
    return false;
}

booWho(null);